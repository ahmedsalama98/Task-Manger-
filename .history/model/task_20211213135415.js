const mongoose = require('mongoose')
const {schema} = mongoose

const taskSchema = {
    name:{
        required:true,
        type:String,
    },
    description:{
        required:true,
        type:String,
        
    },
    name:{
        required:true,
        type:String,
    }

}